<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieCatalog" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/bottom_nav_color</item>
        <item name="colorSecondary">@color/black</item>
        <item name="colorPrimary">@color/main_color</item>
    </style>

    <style name="Theme.MovieCatalog" parent="Base.Theme.MovieCatalog" />
</resources>